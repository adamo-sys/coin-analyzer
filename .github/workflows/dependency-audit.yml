name: Dependency Audit

on:
  push:
    paths:
      - "requirements*.txt"
      - "requirements*.lock"
      - ".github/workflows/dependency-audit.yml"
  pull_request:
    paths:
      - "requirements*.txt"
      - "requirements*.lock"
      - ".github/workflows/dependency-audit.yml"
  schedule:
    - cron: "17 10 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: dependency-audit-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pip-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Install pinned audit tool
        run: python -m pip install pip-audit==2.10.1

      - name: Audit runtime lock
        run: python -m pip_audit --no-deps -r requirements.lock

      - name: Audit development lock
        run: python -m pip_audit --no-deps -r requirements-dev.lock

      - name: Audit optional AI lock
        run: python -m pip_audit --no-deps -r requirements-ai.lock
