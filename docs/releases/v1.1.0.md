# Coin Analyzer v1.1.0

Status: **release candidate preparation — not yet tagged or published**

Release date: TBD

Release candidate base: `68373d000d87b8e3998fc4946ccd3efe07985d70` (`main`, 2026-09-04)

## Overview

v1.1.0 is the next public minor release after the July 17, 2026 `v1.0.0` GitHub release. The repository is 477 commits ahead of that public tag. This release intentionally freezes new feature work and packages the current additive, backward-compatible product state rather than starting another feature sprint.

## Major changes since public v1.0.0

- Hardened capture-package import with validated preparation, durable persistence, rollback/recovery, and explicit failure boundaries.
- Deterministic image-processing stages integrated into the desktop import path while preserving preview-before-confirmation behavior.
- Expanded OCR review workflow with explicit headless contracts, review-session persistence boundaries, and guarded desktop integration.
- Stronger architecture/privacy boundary checks around OCR/evaluation and legacy recognition paths.
- Broader cross-platform regression coverage and an incremental blocking Pyright boundary.
- Required Gitleaks and Ruff gates plus protected-branch workflow and explicit human merge/release authority.
- Optional OpenAI dependency smoke coverage without making cloud AI a core-runtime requirement.

## Release-candidate validation already observed

The latest authoritative PR CI immediately preceding this release-prep branch completed successfully on Windows and Ubuntu with Python 3.12.

- Full Windows regression: **4,857 tests run, 17 skipped, 0 failures/errors**.
- Focused architecture/privacy boundary suite: **44 tests passed**.
- `test (windows-latest)`: PASS.
- `test (ubuntu-latest)`: PASS.
- `ruff`: PASS.
- `gitleaks`: PASS.
- `pyright-event-bus`: PASS.
- Optional OpenAI dependency smoke: PASS.
- Quality Advisory workflow: PASS.

This evidence belongs to the immediately preceding candidate base/merge validation. The release-prep PR must itself complete the required checks before it can be considered tag-ready.

## Remaining release gates

- [ ] Release-prep PR required checks pass.
- [ ] README regression counts are reconciled with final release-candidate CI evidence.
- [ ] Native Windows/Tk smoke test passes using `coin_collection_gui.py`.
- [ ] Fresh-environment install/start path is revalidated.
- [ ] Manual smoke confirms launch, empty collection, save/reload, import/export, Portfolio Analytics, and current image/OCR review entry points.
- [ ] Optional-AI-disabled startup is confirmed on the release candidate.
- [ ] Final diff contains no private collection data, credentials, local paths, or uncertain-provenance release artifacts.
- [ ] Final release notes are reviewed against implemented behavior only.
- [ ] Annotated `v1.1.0` tag is explicitly approved by the human maintainer.
- [ ] GitHub release and remote tag target are verified after publication.

## Current boundaries / known limitations

- Windows remains the primary supported desktop platform and native Tcl/Tk behavior requires a manual smoke check.
- Collection storage remains local single-user JSON; users remain responsible for independent backups.
- Optional AI/live-provider behavior requires user-supplied credentials and network access; core collection management does not.
- OCR, image recognition, grading, and related visual inference remain advisory and require collector review.
- Benchmark/evaluation claims remain constrained by the repository's provenance, authorization, and human-review gates; blocked or prospective benchmark work is not promoted to release claims.
- Whole-repository Pyright remains advisory because existing type debt is being reduced incrementally rather than converted into a surprise release blocker.

## Explicitly deferred from v1.1.0

The following quality-ratchet items are not release blockers unless they reveal a concrete defect:

- whole-repository type cleanup;
- broader Ruff rule adoption;
- additional property/mutation-test pilots;
- dependency-scanning experiments;
- CI optimization and auto-merge configuration;
- broad architecture refactors;
- benchmark work that remains blocked by provenance/authorization/evidence requirements.

## Definition of done

v1.1.0 is complete only after the final release candidate is tested, documented, audited, explicitly approved, tagged with an annotated `v1.1.0` tag, published as a GitHub release, and the remote tag target is verified.
